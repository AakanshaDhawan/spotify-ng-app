'use strict';

describe('MusicSearch module', function() {

  beforeEach(module('MusicSearch'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      var mcCtrl = $controller('MusicSearchCtrl');
      expect(mcCtrl).toBeDefined();
    }));

  });
});
